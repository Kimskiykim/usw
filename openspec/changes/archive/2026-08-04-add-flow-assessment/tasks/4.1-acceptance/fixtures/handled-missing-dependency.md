# Flow: handled-missing-dependency

## Dependencies

- bundled skill: `missing-assessment-dependency`

Call `missing-assessment-dependency`.

- If it is available and succeeds, return `completed`.
- If it is unavailable, return `decision_required`.
