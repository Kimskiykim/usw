# Flow: missing-dependency

## Dependencies

- bundled skill: `missing-assessment-dependency`

Call the mandatory `missing-assessment-dependency` skill, then return
`completed`. No fallback is available.
