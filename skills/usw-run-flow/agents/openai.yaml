interface:
  display_name: "Run USW flow"
  short_description: "Run one shared or local validated USW flow action"
  default_prompt: "Run the selected project-owned USW flow with explicit scope and authority checks."
policy:
  allow_implicit_invocation: true
