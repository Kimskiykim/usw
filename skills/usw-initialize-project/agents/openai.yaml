interface:
  display_name: "USW Init"
  short_description: "Initialize OpenSpec and local USW state"
  default_prompt: "Use $usw-initialize-project to initialize an OpenSpec-compatible USW workspace in this project."

policy:
  allow_implicit_invocation: false
