interface:
  display_name: "USW Handoff"
  short_description: "Manage optional local work state"
  default_prompt: "Use $usw-manage-handoff to save, inspect, resume or finish generic developer-local state when enabled."
