interface:
  display_name: "Create USW flow"
  short_description: "Create readable Markdown flows"
  default_prompt: "Use $usw-create-flow to create an ordinary or structured-style Markdown flow without executing it."

policy:
  allow_implicit_invocation: true
