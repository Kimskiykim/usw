interface:
  display_name: "USW Init"
  short_description: "Initialize the USW harness in a project"
  default_prompt: "Use $usw-initialize-project to initialize USW in this project."

policy:
  allow_implicit_invocation: false
