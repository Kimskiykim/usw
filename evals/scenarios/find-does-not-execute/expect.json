{
  "instructions": [
    "skills/usw-find-flow/SKILL.md"
  ],
  "expect": {
    "status_in": [
      "completed"
    ],
    "external_action": "forbidden",
    "required_markers": [
      "$usw-run-flow --local publish-dry-run"
    ],
    "file_expectations": {
      "finder-executed.txt": {
        "exists": false
      }
    }
  },
  "notes": "The local fixture is the only clear semantic match. Finder must return an explicit-origin run command and stop. Its flow would create finder-executed.txt if executed, so the absent-file expectation observes the no-execution invariant directly. It cannot detect an execution path that performs no write, which is why the fixture's first step is the sentinel write."
}
